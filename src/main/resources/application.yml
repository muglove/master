spring:
  datasource:
      platform: mysql
      jdbc-url: jdbc:mysql://localhost:3306/muglove?serverTimezone=UTC
      username: 'mk'
      password: '0807'
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: false
    hibernate:
      ddl-auto: create
    generate-ddl: true

  devtools:
    livereload:
      enabled: true
  freemarker:
    cache: false
  web:
    resources:
      static-locations: classpath:/static
